name: Questions
options:
  bundleIdPrefix: rocks.barry.questions
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.10"
    # Simulator-first: a fresh clone builds with no provisioning profile.
    # `barry ios build --device` layers signing on as xcodebuild overrides.
    CODE_SIGNING_ALLOWED: "NO"
    CODE_SIGN_IDENTITY: ""
targets:
  Questions:
    type: application
    platform: iOS
    sources:
      - App
    info:
      path: App/Info.plist
      properties:
        CFBundleDisplayName: Questions
        UILaunchScreen: {}
        UISupportedInterfaceOrientations: [UIInterfaceOrientationPortrait]
        # Plain HTTP over a tailnet: the service binds loopback and is reached
        # through Caddy, which serves questions.barry.lan with an internal CA
        # the phone does not trust.
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        ITSAppUsesNonExemptEncryption: false
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        TARGETED_DEVICE_FAMILY: "1"
        PRODUCT_BUNDLE_IDENTIFIER: rocks.barry.questions.app
  QuestionsTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - Tests
    dependencies:
      - target: Questions
schemes:
  Questions:
    build:
      targets:
        Questions: all
        QuestionsTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - QuestionsTests
